# TeleCoder Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# --- Required ---

# GitHub personal access token (needs repo scope).
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# At least one LLM API key is required.
ANTHROPIC_API_KEY=sk-ant-your_key_here
# OPENAI_API_KEY=sk-your_key_here

# --- Telegram Bot (recommended -- easiest phone access) ---

# Get this from @BotFather on Telegram (takes 30 seconds).
TELEGRAM_BOT_TOKEN=123456789:ABCdefGhIjKlMnOpQrStUvWxYz

# Default repo so you don't need --repo every time.
TELEGRAM_DEFAULT_REPO=your-org/your-repo

# --- GitHub Webhook (optional -- enables PR comment handling) ---

# Shared secret for verifying GitHub webhook payloads (HMAC-SHA256).
# Set this in your GitHub repo webhook settings under "Secret".
# When configured, TeleCoder will listen for PR comments and spin up
# an agent to address them automatically.
# GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# --- Slack Bot (optional -- for team use) ---

# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...
# SLACK_DEFAULT_REPO=your-org/your-repo
